# Bind to all network interfaces so Docker can route traffic to it
bind 0.0.0.0

# Turn off protected mode so external applications can connect
protected-mode no

# Require a password to connect to this Redis instance
requirepass MySecurePassword123!

# Enable Append Only File (AOF) persistence for data safety
appendonly yes
appendfsync everysec
